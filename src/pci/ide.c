
//! c test function, when used it has to be placed in unsafe{}
int c_test_function(unsigned int unsigned_int, signed int signed_int, const char* arrary_of_char) 
{
    
// I don't think we can use printf... We would have to use rust function in c function... For a rust function
// When we have disk handling and userspace programs, we will have to make a libc
    return 
}